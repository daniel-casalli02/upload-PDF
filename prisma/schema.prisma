generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Aluno {
    id        Int      @id @default(autoincrement())
    nome      String
    Escola    String?
    Turma     String?
    Foto      String?

    @@map("exemplo")
}
